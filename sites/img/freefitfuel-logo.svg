<svg xmlns="http://www.w3.org/2000/svg" width="1600" height="340" viewBox="0 0 1600 340">
  <defs>
    <style>
      .w{font-family:Montserrat,Segoe UI,Arial,sans-serif;font-weight:900;letter-spacing:-1.2px}
    </style>
  </defs>

  <!-- ONE HORIZONTAL LINE -->
  <text x="80"  y="168" font-size="156" class="w" fill="#000000">FREE</text>
  <text id="fit" x="650" y="168" font-size="156" class="w" fill="#FF6A00">FIT</text>
  <text x="980" y="168" font-size="156" class="w" fill="#000000">FUEL</text>

  <!-- DUMBBELL UNDER 'FIT' (slightly wider than 'FIT') -->
  <!-- FIT spans approx 650..930; bar extends a touch wider to read as dumbbell, not barbell -->
  <g fill="#000000" aria-label="dumbbell under FIT">
    <!-- handle -->
    <rect x="630" y="206" width="360" height="44" rx="14"/>
    <!-- LEFT plates: larger INNER, smaller OUTER -->
    <!-- inner (larger, close to handle) -->
    <rect x="594" y="190" width="38" height="76" rx="8"/>
    <!-- outer (smaller, further from handle) -->
    <rect x="570" y="198" width="22" height="60" rx="6"/>

    <!-- RIGHT plates: mirror -->
    <!-- inner (larger) -->
    <rect x="992" y="190" width="38" height="76" rx="8"/>
    <!-- outer (smaller) -->
    <rect x="1032" y="198" width="22" height="60" rx="6"/>
  </g>
</svg>
