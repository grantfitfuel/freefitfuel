<?xml version="1.0" encoding="UTF-8"?>
<svg width="2000" height="620" viewBox="0 0 2000 620" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <style>
      .word { font: 800 260px -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Ubuntu, "Helvetica Neue", Arial, sans-serif; }
    </style>
  </defs>

  <!-- TEXT -->
  <text x="80" y="360" class="word">
    <tspan fill="#000000">Free </tspan>
    <tspan fill="#FF6A00">Fit</tspan>
    <tspan fill="#000000"> Fuel</tspan>
  </text>

  <!-- CHUNKY DUMBBELL -->
  <g fill="#000000" transform="translate(0,0)">
    <!-- HANDLE (short + thick) -->
    <rect x="920" y="420" width="200" height="70" rx="35"/>

    <!-- LEFT inner plate -->
    <rect x="860" y="360" width="60" height="190" rx="12"/>
    <!-- LEFT outer plate -->
    <rect x="820" y="390" width="40" height="130" rx="10"/>

    <!-- RIGHT inner plate -->
    <rect x="1120" y="360" width="60" height="190" rx="12"/>
    <!-- RIGHT outer plate -->
    <rect x="1180" y="390" width="40" height="130" rx="10"/>
  </g>
</svg>
